(() => {
  const state={data:null,query:'',category:'all'};
  const el={search:document.getElementById('cic-search'),category:document.getElementById('cic-category'),nav:document.getElementById('cic-category-nav'),catalog:document.getElementById('cic-catalog'),count:document.getElementById('cic-result-count'),filter:document.getElementById('cic-active-filter'),empty:document.getElementById('cic-empty'),updated:document.getElementById('cic-updated'),total:document.getElementById('cic-total'),categories:document.getElementById('cic-total-categories')};
  const norm=(v='')=>v.normalize('NFD').replace(/[\u0300-\u036f]/g,'').toLowerCase();
  const esc=(v='')=>String(v).replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));
  const categoryById=id=>state.data.categorias.find(c=>c.id===id);
  function matches(item){const q=norm(state.query);const hay=norm([item.nome,item.orgao,item.descricao,item.abrangencia,item.documento,item.palavrasChave,categoryById(item.categoria)?.nome].join(' '));return(state.category==='all'||item.categoria===state.category)&&(!q||hay.includes(q));}
  function card(item){return `<article class="cic-card"><div class="cic-card-top"><div><h3>${esc(item.nome)}</h3><span class="cic-org">${esc(item.orgao)}</span></div><button class="cic-favorite" type="button" disabled title="Favoritos disponíveis em futura Área do Cliente" aria-label="Favorito indisponível">☆</button></div><p>${esc(item.descricao)}</p><div class="cic-meta"><span><strong>Abrangência:</strong> ${esc(item.abrangencia)}</span><span><strong>Consulta por:</strong> ${esc(item.documento)}</span><span><strong>Atualizado:</strong> ${new Date(item.atualizadoEm+'T12:00:00').toLocaleDateString('pt-BR')}</span></div><div class="cic-badges"><span class="cic-badge">✓ Fonte oficial</span>${item.destaque?'<span class="cic-badge">★ Acesso prioritário</span>':''}</div><a class="button button--primary" href="${esc(item.url)}" target="_blank" rel="noopener noreferrer">Acessar emissão oficial ↗</a></article>`;}
  function render(){const items=state.data.itens.filter(matches);el.count.textContent=`${items.length} certidão${items.length===1?'':'ões'} encontrada${items.length===1?'':'s'}`;el.filter.textContent=state.category==='all'?'Todas as esferas':categoryById(state.category)?.nome||'';el.empty.hidden=items.length>0;el.catalog.innerHTML=state.data.categorias.map(cat=>{const list=items.filter(i=>i.categoria===cat.id);if(!list.length)return '';return `<section class="cic-section" id="cic-${cat.id}"><div class="cic-section-head"><div class="cic-section-title"><span class="cic-section-icon">${cat.icone}</span><div><h2>${esc(cat.nome)}</h2><p>${esc(cat.descricao)}</p></div></div><span class="cic-count">${list.length} acesso${list.length===1?'':'s'}</span></div><div class="cic-grid">${list.map(card).join('')}</div></section>`}).join('');document.querySelectorAll('#cic-category-nav button').forEach(b=>b.classList.toggle('is-active',b.dataset.category===state.category));el.category.value=state.category;}
  function setCategory(id){state.category=id;render();}
  async function init(){try{const r=await fetch('../../data/certidoes.json',{cache:'no-store'});if(!r.ok)throw new Error('Falha ao carregar');state.data=await r.json();el.total.textContent=state.data.itens.length;el.categories.textContent=state.data.categorias.length;el.updated.textContent=new Date(state.data.atualizadoEm).toLocaleDateString('pt-BR');el.category.innerHTML='<option value="all">Todas as esferas</option>'+state.data.categorias.map(c=>`<option value="${c.id}">${esc(c.nome)}</option>`).join('');el.nav.innerHTML=`<button class="is-active" data-category="all" type="button">Todas</button>`+state.data.categorias.map(c=>`<button data-category="${c.id}" type="button">${c.icone} ${esc(c.nome)}</button>`).join('');el.nav.addEventListener('click',e=>{const b=e.target.closest('button[data-category]');if(b)setCategory(b.dataset.category)});el.category.addEventListener('change',()=>setCategory(el.category.value));el.search.addEventListener('input',()=>{state.query=el.search.value;render()});const hash=location.hash.replace('#','');if(hash.startsWith('cic-'))state.category=hash.slice(4);render();}catch(e){el.catalog.innerHTML='<div class="cic-empty"><strong>Não foi possível carregar a central.</strong><p>Tente novamente em instantes.</p></div>';el.count.textContent='Central temporariamente indisponível';}}
  init();
})();
