(()=>{const d=document.body.dataset;const grid=document.querySelector('#catalog-grid'),search=document.querySelector('#catalog-search'),filter=document.querySelector('#catalog-filter'),empty=document.querySelector('#catalog-empty');let data=[];const n=v=>(v||'').normalize('NFD').replace(/[\u0300-\u036f]/g,'').toLowerCase();const e=v=>String(v||'').replace(/[&<>"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;'}[c]));function render(){const q=n(search.value),f=filter.value;const items=data.filter(x=>(f==='all'||x.categoria===f)&&n(Object.values(x).join(' ')).includes(q));grid.innerHTML=items.map(x=>`<article class="enterprise-card"><small class="audit-badge">✓ Fonte oficial</small><h2>${e(x.nome)}</h2><p>${e(x.descricao)}</p><div class="meta">${e(x.orgao||x.categoria)}</div><a class="button button--primary" href="${e(x.url)}" target="_blank" rel="noopener noreferrer">Acessar serviço oficial ↗</a></article>`).join('');empty.hidden=items.length>0}fetch('../../data/'+d.catalog,{cache:'no-store'}).then(x=>x.json()).then(x=>{data=x.itens;document.querySelector('#catalog-total').textContent=data.length;const cats=[...new Set(data.map(x=>x.categoria))];document.querySelector('#catalog-categories').textContent=cats.length;filter.innerHTML+=[...cats].map(x=>`<option>${e(x)}</option>`).join('');render()}).catch(()=>{empty.hidden=false;empty.textContent='Não foi possível carregar os dados.'});search.addEventListener('input',render);filter.addEventListener('change',render)})();